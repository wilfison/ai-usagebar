{
  "name": "ai-usagebar",
  "version": "0.0.0",
  "private": true,
  "description": "Lint tooling for the AI Usage Bar GNOME Shell extension (dev-only; the extension itself needs no Node runtime).",
  "type": "module",
  "scripts": {
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "eslint": "^9.39.0",
    "globals": "^16.0.0"
  }
}
