/tmp
/docs
/tasks
*.compiled
*.shell-extension.zip

# Local secrets — never commit
*.credentials.json

# Claude Code local scratch state.
.claude/

# Sandbox marker, not part of the project.
.ai-jail
